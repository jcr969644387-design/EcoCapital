name: ecocapital
description: "EcoCapital: aplicación educativa para aprender evaluación de inversiones con escenarios financieros ficticios."
publish_to: "none"

version: 1.0.3+3

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/sounds/
